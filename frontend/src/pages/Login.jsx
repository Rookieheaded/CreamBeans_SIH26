import {useState} from 'react'
import {useLocation,useNavigate} from 'react-router-dom'
import {useAuth} from '../context/AuthContext'
import Icon from '../components/Icon'
export default function Login(){const {login,loading}=useAuth();const nav=useNavigate();const loc=useLocation();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=async e=>{e.preventDefault();setError('');try{await login(email,password);nav(loc.state?.from||'/report',{replace:true})}catch(err){setError(err.message)}};return <section className="auth-page"><div className="auth-card"><span className="kicker">STUDENT ACCESS</span><h1>Welcome back.</h1><p>Sign in with your campus account. Authentication is handled by the FastAPI backend and Supabase.</p><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@college.edu"/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password"/></label>{error&&<div className="form-error"><Icon name="shield" size={17}/>{error}</div>}<button className="btn btn-primary full" disabled={loading}>{loading?'Signing in…':'Sign in'}<Icon name="arrow" size={17}/></button></form><div className="auth-note">Sign in with the account created through the campus authentication service.</div></div></section>}
