export default function ScoreRing({score}){const pct=Math.round((score||0)*100);const r=39,c=2*Math.PI*r,d=c*(1-pct/100);return <div className="score-ring"><svg viewBox="0 0 92 92"><circle className="ring-bg" cx="46" cy="46" r={r}/><circle className="ring-value" cx="46" cy="46" r={r} strokeDasharray={c} strokeDashoffset={d}/></svg><div><strong>{pct}%</strong><small>match</small></div></div>}
